Instruction :

Table: Employee

-- DB BROWSER for SQLite --

CREATE TABLE IF NOT EXISTS Employee 
(	id int,
	name varchar(255),
	department varchar(255),
	managerId int);
	
DELETE FROM Employee;

INSERT INTO Employee (id, name, department, managerId) VALUES 
('101', 'John', 'A', NULL),
('102', 'Dan', 'A', '101'),
('103', 'James', 'A', '101'),
('104', 'Amy', 'A', '101'),
('105', 'Anne', 'A', '101'),
('106', 'Ron', 'B', '101');

Write a solution to find managers with at least five direct reports.
Return the result table in any order.

My solution : 

SELECT t.name
FROM (SELECT e1.name, COUNT(e2.managerId) as direct_report
FROM Employee e1
JOIN Employee e2 ON e1.id = e2.managerId
GROUP BY e1.name) t
WHERE t.direct_report >= 5

-- DID NOT PASSED ALL TESTCASED : same name different manager --

CREATE TABLE IF NOT EXISTS Employee2
(	id int,
	name varchar(255),
	department varchar(255),
	managerId int);

DELETE FROM Employee2;

INSERT INTO Employee2 (id, name, department, managerId) VALUES 
('101', 'John', 'A', NULL),
('102', 'Dan', 'A', '101'),
('103', 'James', 'A', '101'),
('104', 'Amy', 'A', '101'),
('105', 'Anne', 'A', '101'),
('106', 'Ron', 'B', '101'),
('111', 'John', 'A', NULL),
('112', 'Dan', 'A', '111'),
('113', 'James', 'A', '111'),
('114', 'Amy', 'A', '111'),
('115', 'Anne', 'A', '111'),
('116', 'Ron', 'B', '111');

My solution 2 :

SELECT t.name
FROM (SELECT e1.id, e1.name, e1.department,COUNT(e2.managerId) as direct_report
FROM Employee e1
JOIN Employee e2 ON e1.id = e2.managerId
GROUP BY e1.id, e1.department) t
WHERE t.direct_report >= 5

-- All testcases passed --
-- Not quite optimized --

SELECT t.name
FROM (SELECT e1.id, e1.name,COUNT(*) as direct_report
FROM Employee e1
JOIN Employee e2 ON e1.id = e2.managerId
GROUP BY e1.id) t
WHERE t.direct_report >= 5

